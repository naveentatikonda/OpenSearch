---
"Create index":

  - skip:
      version: " - 6.99.99"
      reason:  include_type_name defaults to true before 7.0.0
  - do:
      indices.create:
        index: test_index

  - match: { acknowledged: true }
  - match: { index: "test_index"}

